# Map Function
    # Muito utilizado nas listas
    # Aplicar uma função a um Iterable, por item. (list, tuple, disc e etc.)

lista1 = [1, 2, 3, 4]

#def multi(x):
#    return x * 2

#lista2 = map(lambda x: x*2, lista1) #virou função lambda

print(list(map(lambda x: x*2, lista1)))

# Função map com lambda

